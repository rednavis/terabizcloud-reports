apply plugin: "war"

bootWar {
    mainClassName = "com.terabiz.reports.ReportsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
